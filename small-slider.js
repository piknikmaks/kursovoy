function changeImage(imageName) {
    var image = document.getElementById('myImage');
    image.src = imageName;
  }

function changeImage2(imageName) {
    var image = document.getElementById('myImage2');
    image.src = imageName;
  }

function changeImage3(imageName) {
    var image = document.getElementById('myImage3');
    image.src = imageName;
  }

function changeImage4(imageName) {
    var image = document.getElementById('myImage4');
    image.src = imageName;
  }

function changeImage5(imageName) {
    var image = document.getElementById('myImage5');
    image.src = imageName;
  }

function changeImage6(imageName) {
    var image = document.getElementById('myImage6');
    image.src = imageName;
  }

function changeImage7(imageName) {
    var image = document.getElementById('myImage7');
    image.src = imageName;
  }

function changeImage8(imageName) {
    var image = document.getElementById('myImage8');
    image.src = imageName;
  }

function changeImage9(imageName) {
    var image = document.getElementById('myImage9');
    image.src = imageName;
  }

function changeImage10(imageName) {
    var image = document.getElementById('myImage10');
    image.src = imageName;
  }

function changeImage11(imageName) {
    var image = document.getElementById('myImage11');
    image.src = imageName;
  }

function changeImage12(imageName) {
    var image = document.getElementById('myImage12');
    image.src = imageName;
  }